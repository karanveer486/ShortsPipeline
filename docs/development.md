# Development notes

- Use Python 3.11 or newer.
- Keep third-party dependencies justified and narrowly scoped.
- Do not commit secrets, OAuth tokens, source media, generated media, frames, models, or temporary artifacts.
- Put generated files beneath `workspace/` rather than `src/`, `tests/`, or `docs/`.
- Keep cloud deployment work separate from the local-first pipeline foundation.
